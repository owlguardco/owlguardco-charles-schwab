[build]
builder = "nixpacks"

[deploy]
startCommand = "python3 -m gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120 'dashboard.server:app'"
healthcheckPath = "/api/snapshot"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
