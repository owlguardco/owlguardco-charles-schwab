[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip3 install --break-system-packages -r requirements-railway.txt"]

[start]
cmd = "python3 -m gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120 'dashboard.server:app'"
